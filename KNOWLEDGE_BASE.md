# PCT-CRM888 Knowledge Base

## Error Code data

ข้อมูลหลักอยู่ใน Google Sheet **Error code** และถูกแปลงเป็นไฟล์เว็บ
[`data/error-codes.js`](data/error-codes.js) เพื่อให้ค้นหาได้เร็วและใช้งานแบบ static site ได้โดยไม่ต้องเรียก Google Sheet ทุกครั้งที่เปิดหน้า

ข้อมูลถูกแบ่งเป็น 3 กลุ่ม:

- `ac` — เครื่องปรับอากาศ (แท็บ `AC`)
- `washer` — เครื่องซักผ้า (แท็บ `WM`)
- `fridge` — ตู้เย็น (แท็บ `Ref`)

แต่ละรายการใช้โครงสร้างดังนี้:

```js
{
  id: "ac-f1-1-2",                 // รหัสภายในที่ไม่ซ้ำกัน
  product: "ac",                   // ac | washer | fridge
  category: "เครื่องปรับอากาศ",
  code: "F1.1",
  meaning: "อุณหภูมิภายในห้องมีปัญหา",
  rootCause: "...",                // สาเหตุที่ระบุในชีต
  troubleshootingSteps: ["..."],   // ขั้นตอนที่แยกเป็นลำดับ
  troubleshooting: "...",         // ข้อความต้นฉบับครบถ้วน
  safetyWarnings: ["..."],
  recommendedMaterials: "...",
  severity: "warning",             // critical | warning | standard | info
  keywords: ["F1.1", "เซ็นเซอร์"],
  source: { sheet: "AC", row: 2 }
}
```

## วิธีเพิ่ม แก้ไข หรือลบข้อมูล

1. แก้ข้อมูลใน Google Sheet ต้นทางที่แท็บ `AC`, `WM` หรือ `Ref` โดยคงชื่อคอลัมน์เดิมไว้
2. เปิด command line ที่โฟลเดอร์โปรเจกต์ แล้วรัน:

```text
node tools/build-error-code-data.mjs
```

3. ตรวจข้อความสรุปว่าจำนวนรายการถูกต้อง แล้ว commit ไฟล์ `data/error-codes.js` ที่สร้างใหม่

ไม่ต้องแก้ logic ใน `index.html` เมื่อเพิ่มหรือลบ Error Code และไม่ต้องติดตั้ง package เพิ่มเติม ตัวสร้างข้อมูลใช้ความสามารถที่มากับ Node.js โดยตรง

> `data/error-codes.js` เป็นไฟล์ที่สร้างอัตโนมัติ การแก้ไฟล์นี้โดยตรงจะถูกเขียนทับในการ sync ครั้งถัดไป ควรแก้ Google Sheet ซึ่งเป็นแหล่งข้อมูลหลัก

## กติกาที่ระบบสร้างเพิ่มเพื่อการแสดงผล

- `severity` ถูกจัดกลุ่มจากข้อความในชีตเพื่อใช้เป็นตัวกรองเท่านั้น ไม่ได้แทนระดับความเสี่ยงทางวิศวกรรมอย่างเป็นทางการ
- `safetyWarnings` มีคำเตือนพื้นฐานตามประเภทสินค้า และรวมคำเตือน/หมายเหตุที่พบในข้อมูลต้นฉบับ
- หากแถวใดไม่มีหัวข้อ “สาเหตุ” ระบบจะเว้น `rootCause` ไว้และแสดงข้อความแนวทางเดิมครบถ้วน โดยไม่เดาข้อมูลเพิ่ม
